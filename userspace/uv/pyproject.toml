[project]
name = "agnos"
version = "0.1.0"
authors = [{name = "Vehicle Researcher", email = "user@comma.ai"}]
requires-python = ">=3.12.3, <3.13"
description = ""
dependencies = [
  "openpilot",
  "openpilot[tools,testing,submodules]",
  "msgq",
  "opendbc",
  "pandacan",
  "rednose",
  "teleoprtc",
  "tinygrad",
  "tinymesa==25.2.7.2",

  "pycapnp==2.1.0",  # 2.2 introduces a memory leak due to cyclic references
  "crcmod==1.7",  # C3 panda serial protocol
  "pyserial==3.5",  # C3 panda serial transport
  "kaitaistruct==0.11",  # C3 firmware parsing

  # StarPilot runtime (kept explicit so a clean AGNOS build contains the
  # same managed-venv surface as the validated 19.6.13 image)
  "aiohappyeyeballs==2.7.1",
  "aiohttp==3.12.15",
  "sanic==25.12.1",
  "aiosignal==1.4.0",
  "attrs==26.1.0",
  "frozenlist==1.8.0",
  "json-rpc==1.15.0",
  "mapbox-earcut==1.0.3",
  "multidict==6.7.1",
  "onnx==1.18.0",
  "opencv-python-headless==4.11.0.86",
  "propcache==0.5.2",
  "protobuf==7.35.1",
  "pyaudio==0.2.14",
  "typing-extensions==4.16.0",
  "xattr==1.2.0",
  "yarl==1.24.5",

  # hardware.py
  "dbus-python",
  # amplifier.py
  "smbus2",

  # provisioning
  "pyusb",
  # pandad
  "libusb1 ==3.1.0", # recent versions segfault on comma three
  "paramiko",

  # extra nice-to-haves
  "evdev",
  "ipython",
  "py-spy",
  "spidev2",
  "flaky",
  "matplotlib",

  # GUIs
  "comma-deps-raylib",
]

[tool.uv.sources]
openpilot = { path = "openpilot" }
msgq = { git = "https://github.com/commaai/msgq.git", rev = "master" }
opendbc = { git = "https://github.com/commaai/opendbc.git", rev = "master" }
pandacan = { git = "https://github.com/commaai/panda.git", rev = "master" }
rednose = { git = "https://github.com/commaai/rednose.git", rev = "master" }
teleoprtc = { git = "https://github.com/commaai/teleoprtc.git", rev = "master" }
tinygrad = { git = "https://github.com/tinygrad/tinygrad.git", rev = "master" }
